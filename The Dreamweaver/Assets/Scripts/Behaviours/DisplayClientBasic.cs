using UnityEngine;

public class DisplayClientBasic : MonoBehaviour
{
    public Client client;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
